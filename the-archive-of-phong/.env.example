# Copy to `.env` and fill with your own values before running locally
# Values prefixed with EXPO_PUBLIC_ are embedded in the app at build time.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
